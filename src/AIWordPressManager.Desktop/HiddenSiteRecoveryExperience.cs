namespace AIWordPressManager.Desktop;

/// <summary>
/// Automatic hidden-site recovery was intentionally disabled.
/// Duplicate website records are now handled only through the explicit
/// Open / Soft delete / Physical delete decision window.
/// </summary>
internal static class HiddenSiteRecoveryExperience
{
}
