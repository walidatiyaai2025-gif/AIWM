namespace AIWordPressManager.Desktop;

/// <summary>
/// Live operation status is now rendered inside DockedRightSidebarExperience.
/// The former floating panel is intentionally inactive to avoid duplicate UI,
/// duplicate property subscriptions, and workspace overlap.
/// </summary>
internal static class LiveOperationsExperience
{
}
