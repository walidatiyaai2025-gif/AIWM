<Window x:Class="AIWordPressManager.Desktop.Services.PuterGatewayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Puter AI Connection" Width="980" Height="760"
        MinWidth="760" MinHeight="560" WindowStartupLocation="CenterOwner"
        Background="{DynamicResource SurfaceBrush}">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Puter AI Gateway" FontSize="24" FontWeight="SemiBold"
                   Foreground="{DynamicResource GoldOnLightBrush}"/>
        <TextBlock Grid.Row="1" Margin="0,6,0,12" TextWrapping="Wrap"
                   Text="Sign in with your Puter account. The embedded gateway uses a secure local origin and keeps the WebView2 session for later AI requests."/>

        <Border Grid.Row="2" Margin="0,0,0,12" Padding="12" CornerRadius="8"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
            <WrapPanel>
                <TextBlock x:Name="WebViewHealth" Text="● WebView: checking" Margin="0,0,18,0"/>
                <TextBlock x:Name="CryptoHealth" Text="● Crypto: checking" Margin="0,0,18,0"/>
                <TextBlock x:Name="PuterHealth" Text="● Puter.js: checking" Margin="0,0,18,0"/>
                <TextBlock x:Name="AuthHealth" Text="● Sign-in: checking"/>
            </WrapPanel>
        </Border>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>
            <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="10" ClipToBounds="True">
                <wv2:WebView2 x:Name="Browser"/>
            </Border>
            <Border Grid.Column="1" Margin="12,0,0,0" Padding="12" CornerRadius="10"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Gateway diagnostics" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <TextBox x:Name="DiagnosticsText" IsReadOnly="True" TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto" AcceptsReturn="True"/>
                </DockPanel>
            </Border>
        </Grid>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <TextBlock x:Name="StatusText" VerticalAlignment="Center" Margin="0,0,16,0" Text="Initializing secure Puter gateway..."/>
            <Button Content="Reload" Padding="18,8" Margin="0,0,8,0" Click="Reload_Click"/>
            <Button Content="Sign in / reconnect" Padding="18,8" Margin="0,0,8,0" Click="SignIn_Click"/>
            <Button Content="Hide" Padding="18,8" Click="Hide_Click"/>
        </StackPanel>
    </Grid>
</Window>
