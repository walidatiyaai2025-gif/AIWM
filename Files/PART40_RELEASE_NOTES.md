# Part 40 — Visual Inspector compile fix and offline history

- Fixed the malformed C# raw string in `VisualInspectionService`.
- Visual inspection results are saved locally as JSON after every scan.
- The latest saved results load automatically when the application starts or the site is selected.
- Added HTML report export from the Visual Inspector screen.
- Screenshot evidence remains stored under the application screenshots directory.
