# Part 75 — Global Two-Color Theme and Ready Setup EXE

- Every application surface now derives from two palette colors.
- Added Primary/Secondary tokens and removed remaining fixed gold UI styling.
- Status, grids, tabs, menus, buttons, charts and selections follow the active palette.
- Release build of Setup produces `Setup/Release/AIWordPressManager.Setup.exe`.
- The setup EXE contains the application payload, installs under LocalAppData Programs, creates shortcuts, and launches the app.
